

const Cards = () => {
  return (
    <div className="grid grid-cols-3 gap-6 px-6">
      {/* {Card.map((card, index) => (
        <CardLink key={index} title={card.title} url={card.url} />
      ))} */}
    </div>
  );
};

export default Cards;
