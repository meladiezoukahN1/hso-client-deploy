import UserManagements from "@/components/management/user-managements/UserManagements";
import PageContainer from "@/components/ui/pageContainer";

const page = () => {
  return (
    <PageContainer>
      <UserManagements />
    </PageContainer>
  );
};

export default page;
