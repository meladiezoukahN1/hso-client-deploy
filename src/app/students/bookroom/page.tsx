import BookRoom from "@/components/students/BookRoom";
import PageContainer from "@/components/ui/pageContainer";
const Students = () => {
  return (
    <PageContainer>
      <BookRoom />
    </PageContainer>
  );
};

export default Students;
